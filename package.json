{
  "name": "velostream",
  "version": "2.0.0",
  "description": "VELOSTREAM - High-Performance Screen Sharing Platform (WebRTC + Mediasoup SFU)",
  "private": true,
  "scripts": {
    "start": "cd backend && node server.js",
    "dev": "cd backend && nodemon server.js",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "vercel-build": "echo 'Static frontend — no build needed'"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "webrtc",
    "mediasoup",
    "screen-share",
    "sfu",
    "streaming",
    "nodejs"
  ],
  "author": "yahya308",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
